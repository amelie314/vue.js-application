#app
  button.initial(@click="initial") 發動休學艦
  transition-group.timeline(tag="ul",name="fade")
    li.dialog_wrapper(v-for="l,id in logs",:key="l",:style="{'transition-delay':id/2+'s'}")
      .dialog
        h3.year {{l.year}}
            .decor_bar
        ul.content
          li(v-for="c in l.content") {{c.tag}}
        
      